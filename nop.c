#include "monty.h"
/**
 * f_nop - no operation.
 * @head: double pointer to the head of the stack.
 * @counter: line number where the nop operation is being executed.
 *
 * Return: none
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
}
